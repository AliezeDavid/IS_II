//When I tell persons Java is simple. They think it's a myth however let's go again

public class Vehicle {
    private double Speed;

    //This is the method called drive we were asked to create in the original question
    void drive() {

    }

//This is also the Engine Status we were asked to create. It is this simple
// and it doesn't get any simpler than this
    void Engine_Status() {

    }
    //Now remember in the question we were asked to create an attribute for Speed
    //Now we created a method called speed what
    // and the speed method collects a parameter of type double
    //Now if you followed the formula in the first question,
    // then I said this.attribute name = method argument which is speed in this context
    void Speed(double speed){
        this.Speed = speed;
    }

}
